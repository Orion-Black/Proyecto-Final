package Store;

import java.io.*;
import java.util.*;
/*public class BuscarCancion {
    public static void main(String[] args) throws FileNotFoundException {
        Archivo filex = new Archivo("canciones.txt");
        ArrayList<Cancion> listSongs;
        Scanner tcanciones = new Scanner();
        Scanner entrada = new Scanner(System.in);
        String cancion;
        System.out.print("¿Qué canción deseas buscar? ");
        cancion = entrada.nextLine().toLowerCase();
        System.out.println();
        String linea;
        boolean contiene = false;

        while (tcanciones.hasNext()) { //leer cada linea del archivo
            linea = tcanciones.nextLine();
            if (linea.toLowerCase().contains(cancion)) {   //si la línea contiene el texto buscado se muestra por pantalla
                System.out.println(linea);
                contiene = true;
            }
        } tcanciones.close();
        if (!contiene) { //si el archivo no contienen el texto se muestra un mensaje indicándolo
            System.out.println("No hay coincidencias");
        }
    }
}

         */
